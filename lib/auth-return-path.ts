/** Short-lived cookie: where to send the user after `/auth/callback` (path only). */
export const AUTH_RETURN_PATH_COOKIE = "outbound_crm_auth_next";
export const AUTH_RETURN_PATH_MAX_AGE_SEC = 600;
